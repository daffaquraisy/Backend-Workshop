import { Request, Response, NextFunction } from 'express';
import { dataSource } from '../typeorm.config';
import axios from 'axios';

export const myFunction = async (req: Request, res: Response, _next: NextFunction) => {
  try {
    // code
  } catch (error) {
    // code
  }
}